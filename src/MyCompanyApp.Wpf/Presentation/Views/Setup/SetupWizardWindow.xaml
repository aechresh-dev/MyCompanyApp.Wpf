<Window x:Class="MyCompanyApp.Wpf.Presentation.Views.Setup.SetupWizardWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="First Run Setup" Height="420" Width="480" WindowStartupLocation="CenterScreen">
    <Grid Margin="30">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Text="Create First ADMIN" FontSize="22" FontWeight="Bold" Margin="0,0,0,20"/>

        <StackPanel Grid.Row="1" Margin="0,6">
            <TextBlock Text="Username" FontSize="10" Foreground="Gray"/>
            <TextBox Name="UsernameBox" Height="30"/>
        </StackPanel>

        <StackPanel Grid.Row="2" Margin="0,6">
            <TextBlock Text="Password" FontSize="10" Foreground="Gray"/>
            <PasswordBox Name="PasswordBox" Height="30"/>
        </StackPanel>

        <StackPanel Grid.Row="3" Margin="0,6">
            <TextBlock Text="Confirm Password" FontSize="10" Foreground="Gray"/>
            <PasswordBox Name="ConfirmPasswordBox" Height="30"/>
        </StackPanel>

        <Button Grid.Row="4" Content="Create Admin" Height="36" Margin="0,20,0,0" Name="CreateButton"/>
    </Grid>
</Window>
