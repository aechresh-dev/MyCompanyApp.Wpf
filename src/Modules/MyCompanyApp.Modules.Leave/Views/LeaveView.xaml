<UserControl x:Class="MyCompanyApp.Modules.Leave.Views.LeaveView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="LeftToRight"
             Background="#F8FAFC">
    <Grid Margin="24">
        <Border Background="White" CornerRadius="18" Padding="28">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,16">
                    <StackPanel DockPanel.Dock="Left">
                        <TextBlock Text="{Binding Title}" FontSize="32" FontWeight="Bold" Foreground="#0F172A" />
                        <TextBlock Text="{Binding StatusMessage}" FontSize="15" Foreground="#64748B" Margin="0,8,0,0" TextWrapping="Wrap" />
                    </StackPanel>
                    <Button DockPanel.Dock="Right"
                            Content="Approve One"
                            Command="{Binding ApproveOneCommand}"
                            Width="160"
                            Height="42"
                            Background="#EA580C"
                            Foreground="White"
                            BorderThickness="0"
                            FontWeight="SemiBold" />
                </DockPanel>

                <Border Grid.Row="1" Background="#FFF7ED" CornerRadius="14" Padding="14" Margin="0,0,0,16">
                    <StackPanel>
                        <TextBlock Text="Pending Requests" FontSize="14" Foreground="#C2410C" />
                        <TextBlock Text="{Binding PendingRequestsCount}" FontSize="30" FontWeight="Bold" Foreground="#9A3412" />
                    </StackPanel>
                </Border>

                <ListBox Grid.Row="2"
                         ItemsSource="{Binding Requests}"
                         FontSize="15"
                         BorderBrush="#E2E8F0"
                         BorderThickness="1"
                         Padding="8" />
            </Grid>
        </Border>
    </Grid>
</UserControl>