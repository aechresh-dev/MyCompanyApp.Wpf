<UserControl x:Class="MyCompanyApp.Modules.Users.Views.UsersView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             FlowDirection="LeftToRight"
             Background="#F8FAFC">
    <Grid Margin="24">
        <Border Background="White" CornerRadius="18" Padding="28">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <DockPanel Grid.Row="0" Margin="0,0,0,16">
                    <StackPanel DockPanel.Dock="Left">
                        <TextBlock Text="{Binding Title}" FontSize="32" FontWeight="Bold" Foreground="#0F172A" />
                        <TextBlock Text="{Binding StatusMessage}" FontSize="15" Foreground="#64748B" Margin="0,8,0,0" TextWrapping="Wrap" />
                    </StackPanel>
                    <Button DockPanel.Dock="Right"
                            Content="Select First User"
                            Command="{Binding SelectFirstUserCommand}"
                            Width="160"
                            Height="42"
                            Background="#16A34A"
                            Foreground="White"
                            BorderThickness="0"
                            FontWeight="SemiBold" />
                </DockPanel>

                <Border Grid.Row="1" Background="#F1F5F9" CornerRadius="14" Padding="14" Margin="0,0,0,16">
                    <TextBlock Text="{Binding SelectedUserName, StringFormat=Selected user: {0}}" FontSize="15" Foreground="#334155" />
                </Border>

                <ListBox Grid.Row="2"
                         ItemsSource="{Binding Users}"
                         SelectedItem="{Binding SelectedUserName, Mode=TwoWay}"
                         FontSize="15"
                         BorderBrush="#E2E8F0"
                         BorderThickness="1"
                         Padding="8" />
            </Grid>
        </Border>
    </Grid>
</UserControl>